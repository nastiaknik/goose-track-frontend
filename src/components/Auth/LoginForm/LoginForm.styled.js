import styled from "styled-components";
import {
  StyledForm,
  Title,
  StyledLabel,
  StyledLabelPassword,
  StyledInput,
  SubmitBtn,
} from "../RegisterForm/RegisterForm.styled";

export const StyledLoginForm = styled(StyledForm)``;

export const LoginTitle = styled(Title)``;

export const LoginLabel = styled(StyledLabel)``;

export const LoginPasswordLabel = styled(StyledLabelPassword)``;

export const LoginInput = styled(StyledInput)``;

export const LoginSubmitBtn = styled(SubmitBtn)``;
